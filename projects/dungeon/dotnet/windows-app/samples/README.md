# Windows App Map Samples

This folder is reserved for Windows-specific map rendering samples (e.g., Braille/ANSI/Sixel/Kitty viewers and Avalonia/Skia demos) that mirror the console-app `samples` structure.

Sample projects will be added here as RFC-058 map sample applications are implemented.
