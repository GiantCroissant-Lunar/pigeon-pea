# Development dependencies for pigeon-pea project
# These packages are required for pre-commit hooks and development tooling

# YAML parsing for agent/skill manifests
pyyaml>=6.0,<7.0

# JSON Schema validation for agent/skill schemas
jsonschema>=4.0,<5.0
