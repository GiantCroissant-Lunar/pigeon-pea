using Xunit;

namespace PigeonPea.Console.Tests.Rendering;

[CollectionDefinition("Rendering Tests", DisableParallelization = true)]
public class RenderingTestCollection
{
    // This class has no code, and is never created. Its purpose is
    // simply to be the place to apply [CollectionDefinition] and
    // all the settings.
}
